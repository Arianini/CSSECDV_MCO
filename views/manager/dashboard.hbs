<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Manager Dashboard - TikTalk</title>
    <link rel="stylesheet" href="/styles/PageTemplate.css">
    <link rel="stylesheet" href="/styles/PostTemplate.css">
    <style>
        .manager-container {
            max-width: 1400px;
            margin: 20px auto;
            padding: 20px;
        }

        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }

        .stat-card {
            background: #1f1f1f;
            padding: 20px;
            border-radius: 10px;
            text-align: center;
        }

        .stat-card h3 {
            margin: 0;
            color: #888;
            font-size: 14px;
            text-transform: uppercase;
        }

        .stat-card .number {
            font-size: 36px;
            font-weight: bold;
            color: #ffc107;
            margin: 10px 0;
        }

        .tags-list {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
            justify-content: center;
            margin-top: 10px;
        }

        .tag-badge {
            background: #667eea;
            padding: 5px 15px;
            border-radius: 15px;
            font-size: 14px;
        }

        .manager-nav {
            display: flex;
            gap: 15px;
            margin-bottom: 30px;
            flex-wrap: wrap;
        }

        .manager-nav a {
            padding: 12px 24px;
            background: #ffc107;
            color: #000;
            text-decoration: none;
            border-radius: 5px;
            font-weight: bold;
            transition: 0.3s;
        }

        .manager-nav a:hover {
            background: #ffb300;
        }

        .section-title {
            font-size: 24px;
            margin-bottom: 20px;
            border-bottom: 2px solid #ffc107;
            padding-bottom: 10px;
        }

        .posts-grid {
            display: grid;
            gap: 20px;
        }

        .moderate-card {
            background: #1f1f1f;
            padding: 20px;
            border-radius: 10px;
            border-left: 4px solid #ffc107;
        }

        .moderate-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 15px;
        }

        .moderate-actions {
            display: flex;
            gap: 10px;
        }

        .btn {
            padding: 8px 16px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            text-decoration: none;
            display: inline-block;
            transition: 0.3s;
        }

        .btn-edit {
            background: #17a2b8;
            color: white;
        }

        .btn-delete {
            background: #dc3545;
            color: white;
        }

        .btn:hover {
            opacity: 0.8;
        }
    </style>
    <script src="https://kit.fontawesome.com/2616052655.js" crossorigin="anonymous"></script>
</head>
<body>
    <div class="manager-container">
        <h1>🛡️ Manager Dashboard</h1>
        <p>Welcome, <strong>{{userProfile.username}}</strong> (Moderator)</p>

        <div class="stats-grid">
            <div class="stat-card">
                <h3>Posts to Moderate</h3>
                <div class="number">{{stats.managedPosts}}</div>
            </div>
            <div class="stat-card">
                <h3>Managed Tags</h3>
                <div class="number">{{stats.managedTags}}</div>
                <div class="tags-list">
                    {{#each stats.tags}}
                        <span class="tag-badge">{{this}}</span>
                    {{/each}}
                </div>
            </div>
        </div>

        <div class="manager-nav">
            {{#each stats.tags}}
                <a href="/manager/posts/{{this}}">
                    <i class="fa-solid fa-tag"></i> {{this}}
                </a>
            {{/each}}
            <a href="/home" style="background: #667eea; color: white;">
                <i class="fa-solid fa-home"></i> Back to Home
            </a>
        </div>

        <h2 class="section-title">Recent Posts in Your Scope</h2>
        
        {{#if posts.length}}
        <div class="posts-grid">
            {{#each posts}}
            <div class="moderate-card">
                <div class="moderate-header">
                    <div>
                        <strong>{{user.username}}</strong> · 
                        <span style="color: #ffc107;">{{postTag}}</span> · 
                        <span style="color: #888;">{{formatDate createdAt "YYYY-MM-DD HH:mm"}}</span>
                    </div>
                    <div class="moderate-actions">
                        <a href="/home" class="btn btn-edit">
                            <i class="fa-solid fa-eye"></i> View
                        </a>
                    </div>
                </div>
                <div>
                    <p>{{caption}}</p>
                    {{#if imageUrl}}
                        <img src="{{imageUrl}}" alt="Post" style="max-width: 300px; border-radius: 10px; margin-top: 10px;">
                    {{/if}}
                </div>
                <div style="margin-top: 15px; color: #888;">
                    <i class="fa-solid fa-thumbs-up"></i> {{likesCount}} · 
                    <i class="fa-solid fa-thumbs-down"></i> {{dislikesCount}} · 
                    <i class="fa-solid fa-comment"></i> {{comments.length}} comments
                </div>
            </div>
            {{/each}}
        </div>
        {{else}}
        <div style="text-align: center; padding: 40px; background: #1f1f1f; border-radius: 10px;">
            <i class="fa-solid fa-inbox" style="font-size: 48px; color: #666; margin-bottom: 20px;"></i>
            <p style="color: #888; font-size: 18px;">No posts to moderate in your scope</p>
        </div>
        {{/if}}
    </div>

    <footer>
        <p>&copy; 2025 TikTalk Archers</p>
    </footer>
</body>
</html>