<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Manager Dashboard - TikTalk</title>
    <link rel="stylesheet" href="/styles/PageTemplate.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        .manager-container {
            max-width: 1200px;
            margin: 20px auto;
            padding: 20px;
        }

        h1 {
            color: #667eea;
            margin-bottom: 10px;
            font-size: 32px;
        }

        .welcome-text {
            color: #aaa;
            margin-bottom: 30px;
            font-size: 16px;
        }

        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }

        .stat-card {
            background: #1f1f1f;
            padding: 20px;
            border-radius: 10px;
            text-align: center;
            border: 1px solid #333;
            transition: transform 0.2s;
        }

        .stat-card:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 15px rgba(102, 126, 234, 0.3);
        }

        .stat-card h3 {
            color: #888;
            font-size: 14px;
            margin: 0 0 10px 0;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .stat-card .number {
            font-size: 36px;
            font-weight: bold;
            color: #667eea;
            margin: 10px 0;
        }

        .stat-card .badge {
            background: #f44336;
            color: white;
            border-radius: 12px;
            padding: 2px 8px;
            font-size: 12px;
            font-weight: bold;
            margin-left: 5px;
        }

        .tags-list {
            display: flex;
            flex-wrap: wrap;
            gap: 8px;
            justify-content: center;
            margin-top: 12px;
        }

        .tag-badge {
            background: #667eea;
            padding: 5px 12px;
            border-radius: 15px;
            font-size: 12px;
            color: white;
            font-weight: 500;
        }

        .action-section {
            background: #1f1f1f;
            padding: 30px;
            border-radius: 15px;
            text-align: center;
            margin-bottom: 30px;
            border: 2px solid #667eea;
        }

        .action-section h2 {
            color: #fff;
            margin-bottom: 20px;
            font-size: 24px;
        }

        .btn-primary {
            display: inline-flex;
            align-items: center;
            gap: 10px;
            padding: 18px 40px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            text-decoration: none;
            border-radius: 10px;
            font-weight: bold;
            font-size: 18px;
            transition: all 0.3s;
            border: none;
            cursor: pointer;
        }

        .btn-primary:hover {
            transform: translateY(-3px);
            box-shadow: 0 6px 20px rgba(102, 126, 234, 0.5);
        }

        .btn-primary i {
            font-size: 20px;
        }

        .recent-activity {
            background: #1f1f1f;
            padding: 25px;
            border-radius: 15px;
            margin-bottom: 30px;
        }

        .recent-activity h2 {
            color: #667eea;
            margin-bottom: 20px;
            font-size: 22px;
            border-bottom: 2px solid #667eea;
            padding-bottom: 10px;
        }

        .post-card {
            background: #2a2a2a;
            padding: 20px;
            border-radius: 10px;
            margin-bottom: 15px;
            border-left: 4px solid #667eea;
            transition: all 0.3s;
        }

        .post-card:hover {
            transform: translateX(5px);
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.3);
        }

        .post-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 12px;
        }

        .post-author {
            color: #fff;
            font-weight: bold;
            font-size: 16px;
        }

        .post-tag {
            background: #667eea;
            color: white;
            padding: 4px 12px;
            border-radius: 15px;
            font-size: 12px;
            font-weight: 500;
        }

        .post-caption {
            color: #ddd;
            margin: 10px 0;
            line-height: 1.5;
        }

        .post-image {
            max-width: 200px;
            border-radius: 8px;
            margin-top: 10px;
        }

        .post-stats {
            color: #888;
            font-size: 14px;
            margin-top: 10px;
            display: flex;
            gap: 15px;
        }

        .post-stats i {
            color: #667eea;
        }

        .empty-state {
            text-align: center;
            padding: 60px 20px;
            background: #1f1f1f;
            border-radius: 15px;
        }

        .empty-state i {
            font-size: 64px;
            color: #555;
            margin-bottom: 20px;
        }

        .empty-state h3 {
            color: #fff;
            margin-bottom: 10px;
            font-size: 22px;
        }

        .empty-state p {
            color: #888;
            font-size: 16px;
        }

        @media (max-width: 768px) {
            .stats-grid {
                grid-template-columns: 1fr;
            }

            .btn-primary {
                width: 100%;
                justify-content: center;
            }
        }
    </style>
</head>
<body>
    <div class="manager-container">
        <h1>ðŸ“Š Manager Dashboard</h1>
        <p class="welcome-text">Welcome, <strong>{{userProfile.username}}</strong></p>

        <!-- Stats Section -->
        <div class="stats-grid">
            <div class="stat-card">
                <h3>Managed Posts</h3>
                <div class="number">{{stats.managedPosts}}</div>
                <p style="color: #888; font-size: 12px;">Posts in your scope</p>
            </div>

            <div class="stat-card">
                <h3>Managed Tags</h3>
                <div class="number">{{stats.managedTags}}</div>
                <div class="tags-list">
                    {{#each stats.tags}}
                        <span class="tag-badge">#{{this}}</span>
                    {{/each}}
                </div>
            </div>

            <div class="stat-card">
                <h3>Pending Reports</h3>
                <div class="number">
                    {{stats.pendingReports}}
                    {{#if stats.pendingReports}}
                        <span class="badge">!</span>
                    {{/if}}
                </div>
                <p style="color: #888; font-size: 12px;">Reports awaiting review</p>
            </div>
        </div>

        <!-- Main Action Section -->
        <div class="action-section">
            <h2>🚩 Moderation Center</h2>
            <p style="color: #aaa; margin-bottom: 25px; font-size: 16px;">
                Review and handle reported posts in your managed categories
            </p>
            <a href="/manager/reports" class="btn-primary">
                <i class="fas fa-flag"></i>
                Check Reports & Moderation
                {{#if stats.pendingReports}}
                    <span class="badge">{{stats.pendingReports}}</span>
                {{/if}}
            </a>
        </div>

        <!-- Recent Activity Section -->
        <div class="recent-activity">
            <h2>ðŸ“ Recent Posts in Your Scope</h2>
            
            {{#if posts.length}}
                {{#each posts}}
                    {{#if @index}}
                        {{#if (lt @index 5)}}
                        <div class="post-card">
                            <div class="post-header">
                                <span class="post-author">
                                    <i class="fas fa-user-circle"></i> {{user.username}}
                                </span>
                                <span class="post-tag">#{{postTag}}</span>
                            </div>
                            
                            <p class="post-caption">{{caption}}</p>
                            
                            {{#if imageUrl}}
                                <img src="{{imageUrl}}" alt="Post image" class="post-image">
                            {{/if}}
                            
                            <div class="post-stats">
                                <span><i class="fas fa-thumbs-up"></i> {{likesCount}}</span>
                                <span><i class="fas fa-thumbs-down"></i> {{dislikesCount}}</span>
                                <span><i class="fas fa-comment"></i> {{comments.length}}</span>
                            </div>
                        </div>
                        {{/if}}
                    {{else}}
                        <div class="post-card">
                            <div class="post-header">
                                <span class="post-author">
                                    <i class="fas fa-user-circle"></i> {{user.username}}
                                </span>
                                <span class="post-tag">#{{postTag}}</span>
                            </div>
                            
                            <p class="post-caption">{{caption}}</p>
                            
                            {{#if imageUrl}}
                                <img src="{{imageUrl}}" alt="Post image" class="post-image">
                            {{/if}}
                            
                            <div class="post-stats">
                                <span><i class="fas fa-thumbs-up"></i> {{likesCount}}</span>
                                <span><i class="fas fa-thumbs-down"></i> {{dislikesCount}}</span>
                                <span><i class="fas fa-comment"></i> {{comments.length}}</span>
                            </div>
                        </div>
                    {{/if}}
                {{/each}}
            {{else}}
                <div class="empty-state">
                    <i class="fas fa-inbox"></i>
                    <h3>No Recent Activity</h3>
                    <p>No posts have been created in your managed categories yet</p>
                </div>
            {{/if}}
        </div>
    </div>
</body>
</html>
