<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard</title>
    <link rel="stylesheet" href="/styles/PageTemplate.css">
    <style>
        body {
            background: #121212;
            color: white;
            font-family: Arial, sans-serif;
            padding: 20px;
        }
        .container {
            max-width: 1200px;
            margin: 0 auto;
        }
        h1 {
            color: #dc3545;
            margin-bottom: 10px;
        }
        .stats {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin: 30px 0;
        }
        .stat-card {
            background: #1f1f1f;
            padding: 20px;
            border-radius: 10px;
            text-align: center;
        }
        .stat-card h3 {
            color: #888;
            font-size: 14px;
            margin: 0 0 10px 0;
        }
        .stat-card .number {
            font-size: 36px;
            font-weight: bold;
            color: #dc3545;
        }
        .nav-buttons {
            display: flex;
            gap: 15px;
            margin: 30px 0;
        }
        .nav-buttons a {
            padding: 12px 24px;
            background: #dc3545;
            color: white;
            text-decoration: none;
            border-radius: 5px;
        }
        .nav-buttons a:hover {
            background: #c82333;
        }
        .logs-table {
            background: #1f1f1f;
            border-radius: 10px;
            padding: 20px;
            margin-top: 30px;
        }
        table {
            width: 100%;
            border-collapse: collapse;
        }
        th {
            background: #dc3545;
            padding: 12px;
            text-align: left;
        }
        td {
            padding: 10px;
            border-bottom: 1px solid #333;
        }
        tr:hover {
            background: #2a2a2a;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>🛡️ Admin Dashboard</h1>
        <p>Welcome, <strong>{{userProfile.username}}</strong></p>

        <div class="stats">
            <div class="stat-card">
                <h3>Total Users</h3>
                <div class="number">{{stats.totalUsers}}</div>
            </div>
            <div class="stat-card">
                <h3>Total Posts</h3>
                <div class="number">{{stats.totalPosts}}</div>
            </div>
            <div class="stat-card">
                <h3>Administrators</h3>
                <div class="number">{{stats.totalAdmins}}</div>
            </div>
            <div class="stat-card">
                <h3>Managers</h3>
                <div class="number">{{stats.totalManagers}}</div>
            </div>
            <div class="stat-card">
                <h3>Regular Users</h3>
                <div class="number">{{stats.totalRegularUsers}}</div>
            </div>
        </div>

        <div class="nav-buttons">
            <a href="/admin/users">👥 Manage Users</a>
            <a href="/admin/logs">📋 Audit Logs</a>
            <a href="/home">🏠 Back to Home</a>
        </div>

        <div class="logs-table">
            <h2>Recent Activity</h2>
            <table>
                <thead>
                    <tr>
                        <th>Time</th>
                        <th>User</th>
                        <th>Action</th>
                        <th>Details</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each recentLogs}}
                    <tr>
                        <td>{{formatDate timestamp "YYYY-MM-DD HH:mm"}}</td>
                        <td>{{username}}</td>
                        <td><strong>{{action}}</strong></td>
                        <td>{{details}}</td>
                    </tr>
                    {{else}}
                    <tr>
                        <td colspan="4" style="text-align: center; color: #888;">No activity yet</td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
        </div>
    </div>
</body>
</html>